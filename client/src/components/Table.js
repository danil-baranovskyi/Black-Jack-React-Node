import React from "react";
import {Player} from "./Player.js";

export const Table = () => {
    return (
        <section className="table">
                <Player/>
                <Player/>
                <Player/>
                <Player/>
        </section>
    );
}